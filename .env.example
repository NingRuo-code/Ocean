# Ocean Front Offline Analysis runtime configuration

# Default: deterministic offline parser, no model service required.
OCEAN_AI_PROVIDER=rules

# Ollama example:
# OCEAN_AI_PROVIDER=ollama
# OCEAN_LOCAL_LLM_ENDPOINT=http://127.0.0.1:11434/api/generate
# OCEAN_LOCAL_LLM_MODEL=qwen2.5:1.5b-instruct

# llama.cpp server example:
# OCEAN_AI_PROVIDER=llamacpp
# OCEAN_LOCAL_LLM_ENDPOINT=http://127.0.0.1:8080/completion
# OCEAN_LOCAL_LLM_MODEL=local-gguf

# OpenAI-compatible local server example:
# OCEAN_AI_PROVIDER=openai-compatible
# OCEAN_LOCAL_LLM_ENDPOINT=http://127.0.0.1:8001/v1/chat/completions
# OCEAN_LOCAL_LLM_MODEL=qwen2.5-1.5b-instruct

OCEAN_AI_TIMEOUT_SECONDS=3

# Local model diagnostic:
# cd backend
# python scripts/check_local_llm.py --run-parser

# Server data source credentials.
#
# Fill these only in a local .env or server environment. Never commit .env or
# real token values. Job records only log whether a credential is configured.
OCEAN_SOURCE_GFW_EFFORT_PUBLIC_PLANNED_CREDENTIAL=
OCEAN_SOURCE_PARTNER_AIS_DERIVATIVE_PLANNED_CREDENTIAL=
