
# 信息架构与交互规范（v1.8）

> 日期：2026-09-14
> 适用范围：`prototype-fishing.html` / `prototype-fishing.js`
> 目的：把「渔场作业」的真实决策顺序固化为界面层级，消除冗余与主次不清
>
> **v1.8 变更**：右侧面板进一步减负：当前页默认两张卡，海况安全和全部锋面清单折叠；预测页合并为一张卡，逐日明细折叠；AI 分析合并为一张复核卡，任务编排折叠。
>
> **v1.7 变更**：出发地增加“点选”模式，地图点击可直接更新定位真源，坐标输入兼容方向标注与中文逗号；推荐水域示例层默认关闭；预测页文案改为“规则预测参考”；历史连续窗口、当月和整年视图增加日期格子图。
>
> **v1.6 变更**：顶栏出海日组增加播放 / 暂停和时间轴拖动，仍只写同一个 `state.date`；历史页补齐 7 日、15 日连续实况窗口；预测页增加 1 / 3 / 7 天窗口切换和预测依据展开；AI 分析增加证据链展开。
>
> **v1.5 变更**：按 9.3 沟通记录将右侧页签落地为「当前 / 历史 / 预测 / AI 分析」；「数据说明」改为页脚支撑入口。历史页新增当日 / 近三日 / 当月 / 整年四档；预测页给出 7 天规则预测参考，并明确不等于业务预报；AI 分析限定为本地证据组织与任务编排，不生成新的科学数值。
>
> **v1.1 变更**：① 时间维度收敛为顶栏「出海日」单控件（删掉"观测/预报/气候"三档，"气候"改名「往年同期」只留在该页）；② 新增**地图指针查询**（鼠标指到哪看哪的数据，可钉住）；③ 全站术语平实化（见 §7）；④ 选中态增加回执卡；⑤ 顶栏控件按使用顺序编号分组。
>
> **v1.3 变更**：① **目标鱼种下线**（顶栏由 4 组收敛为 3 组：① 从哪出发 ② 找多远 ③ 出海日；把握评分删除「鱼种偏好侧别」项，冷暖侧只展示）；② 理由：没有真实渔获/适温数据时，规则偏好会被读成预测——页面所有数字只由真实锋面数据算出；③ **底图可读性**：陆地改浅色实色块 + 亮海岸线、默认视野拉远到海岸线在画面内；④ **页面文案平实化**：去掉重复解释与"自我说明"式句子，卡片只留结论与数字，口径集中在「依据」页。
>
> **v1.4 变更**：**界面文案改成商用平台口吻**（术语见 §7.1）：锋面对象→锋面区、海表温度→水温、找鱼范围→作业范围、观测覆盖→数据覆盖、把握→把握度、值得去的水域→推荐水域、页签「往年同期/依据」→「历史同期/数据说明」、时段按钮→当日/近三日/当月；界面不再出现像元计数与开发史叙述（如"以前按衰减推过数字"）。

---

## 1. 市场与用户需求调研（结论）

### 1.1 调研来源（可核查）

| 来源 | 关键事实 |
|---|---|
| FishTrack（消费级海钓预报 App，Surfline/Wavetrak）| 首页即"找鱼工具"：**无云 SST 图 + 叶绿素 + 真彩影像 + 流场/潮汐/月相 + 7 天海洋天气**；配套**收藏点、温跃层断点标记、航点/航线、离线图**；卖点原话 **"Save time and fuel"**（省油＝省钱，即可达性）；用户原话："SST 和海洋预报是我早上最先看的两样东西……水温断点和风/流读数决定我怎么过这一天" |
| NOAA CoastWatch 产品检索 | 锋面是**业务化产品**：`ACSPO 0.02° Super-collated SST and Thermal Fronts (L3S-LEO)`，分辨率 2 km+，时效 NRT ≤24 h；另有**叶绿素异常产品**（相对 61 天中位数），说明"锋面 + 叶绿素边缘"可直接上图 |
| NOAA TurtleWatch（太平洋岛屿）| 典型的"**锋面直接产出空间指引**"形态：用特定 SST 等温线/锋面 + 水深界线圈定可用海域（具体阈值待核实）|
| Global Fishing Watch | 定位为**捕捞活动/热点 + 管辖区图层**（事后活动分布，属"验证类"信息，非"明天去哪"的预判）|
| OpenAlex 文献检索 | ① *SST fronts affect distribution of Pacific saury*（2014，被引 56）——锋面→物种栖息分布；② *Forecasting Albacore Fishing Grounds … ML + Ensemble*（2023）；③ *Ensemble Modelling of Skipjack Tuna Habitats*（2020，被引 51）；④ *Towards a Fishing Pressure Prediction System for a Western Pacific EEZ*（2019）；⑤ *Marine heatwaves redistribute pelagic fishing fleets*（2024）——**异常年份整体改变作业分布**；⑥ *Fishing for what? understanding fisher decision-making*（2010）——渔民决策＝经济+社会+环境混合；⑦ *Smartphone application use in commercial wild catch fisheries*（2022）；⑧ 东海相关：*Swordtip Squid Habitat（ECS, 2023）*、*Largehead Hairtail Nursery Grounds（ECS, 2022）* |

> 抓取受限（未取得内容，故不作依据）：`roffs.com`、`ripcharts.com`、`bom.gov.au`、`nmefc.cn`、`oceanguide.cn`、`eastfishery.ac.cn`、Bing/DuckDuckGo。中国本土"渔情速报/渔场预报"产品细节**暂无一手证据**，下文涉及处均已标注推断。

### 1.2 用户决策链（UI 层级据此排序）

```
第 0 问  今天/这几天能不能出海？   ← 安全（风/浪/涌）+ 成本（油）+ 法规（禁渔期）
第 1 问  去哪一片？              ← 渔场线索排序（锋面/温跃层/叶绿素边缘）+ 方位 + 距离
第 2 问  怎么去、多久到？         ← 航距 → 航时/油耗
第 3 问  什么时候最合适？         ← 未来 1/3/7 天窗口 + 逐日变化
第 4 问  为什么推荐这儿、可靠吗？  ← 历史同期规律 + 算法依据 + 异常提示
第 5 问  这一片到底怎么样？       ← 地图上任意点查询（v1.1 新增，见 §3.2 / FR-13）
```

### 1.3 需求分级

- **Must**：结论先行的一句话建议；可执行点位（方位+距离）；可达性（航时/油耗）；时间窗；安全/海况；可解释依据
- **Should**：历史同期规律与异常提示；目标鱼种相关权重（v1.3 暂缓，等真实渔获/适温数据）；图层可控 + 时效标识；无数据时的诚实回退；任意点查询
- **Could（本期不做）**：AIS 活动层、三维球面、离线包、分享导出、真实大模型

---

## 2. 重构前的诊断（问题 → 结论）

### 2.1 第一轮（v0.3 已解决）

| # | 现象（改造前） | 问题本质 |
|---|---|---|
| 1 | 四个等权页签 `flex:1`，"渔场可能度/建议"散落在「预测」与「AI 分析」各写一遍 | 页签是**模块名**不是**任务**，没有重点 |
| 2 | 「AI 分析」页有「可信度」卡 | 与本仓需求文档 §3.2「不再展示多重可信度标签」**自相矛盾**，且与「预测」页职责重叠 |
| 3 | 顶栏 4 档尺度 + 历史页自带周期选择器 | **两套时间真源**；`fillNowList()` 按尺度切换内容导致"当前"页显示月/年统计 → 语义坍塌 |
| 4 | 缺安全/海况、航距/航时/油耗、目标鱼种、一句话结论、无数据态；却有像元计数等过程数据 | 该有的没有，不该有的占位（违反 §3.2 精简原则与 FR-7 空态要求） |
| 5 | 条目高亮规则不统一（`hl` 字符串 + 两套映射 + coldwarm 特例） | 交互语义不收敛，同一类信息两种行为 |

### 2.2 第二轮（v1.1 解决）

| # | 现象（v0.3 之后仍存在） | 问题本质 |
|---|---|---|
| 6 | 顶栏 5 组控件平铺无分组、无先后；时间能改的地方有 **3 处**（顶栏日期、"观测/预报/气候"三档、规律页 按日/按月/按年 + 3 个日期输入框） | **没有顺序**，用户不知道从哪开始；同一件事多个入口 |
| 7 | "气候"档切过去后，结论卡照样算"可出海/把握"、照样给"风力 4 级/浪高 1.2 m" | **语义错位 + 编数据**：气候态没有"今天几点风多大" |
| 8 | 页签"现在/未来"与顶栏"观测/预报"是同一件事的两种说法 | 两套导航打架 |
| 9 | 地图只能点既有对象（锋面/渔场区），任意位置没有数值；只有热力图看不出"这里多少度、离我多远" | 渔民最常问的"这一片怎么样"无法回答 |
| 10 | 选中后只有地图压暗，没有"我选了什么、怎么取消" | 反馈缺回执 |
| 11 | 术语偏学术：「气候态、结论口径、适航判据、时次、温度梯度、演示占位」 | 与渔民日常说法脱节 |
| 12 | 渔场区椭圆的半轴写成**像素**，卡片却用公里 | 地图上看到的范围和卡片数字是两套说法（几何隐患） |

---

## 3. 目标信息架构（三层）

### 3.1 层级与职责

| 层 | 位置 | 职责 | 约束 |
|---|---|---|---|
| **L0 全局上下文** | 顶栏 | ① 从哪出发 ② 找多远 ③ 出海日 | **全局唯一真源**；其它位置不得再出现时间/范围控件 |
| **L1 结论层** | 侧栏顶部 hero | 一句话结论（可否出海）+ 首选/备选点位 + 「为什么这么判断」 | 常驻，**不随页签消失**；无数据时明确说明且不给结论 |
| **L2 分层细节** | 页签 | 当前 / 历史 / 预测 / AI 分析；数据说明作为页脚支撑入口 | 四个页签同级，避免老师沟通中要求保留的功能入口被隐藏 |

```
┌──────────────────────────────────────────────────────────────────────────┐
│ L0 ⛵渔场向导 ①从哪出发[坐标/定位/点选] ②找多远[10|20|30] ③出海日[◀日期▶]    │
├──────────────────────────────────────┬───────────────────────────────────┤
│                                      │ L1 值不值得去（hero 常驻）          │
│                                      │   先去 东北 16.8 km 的 S1          │
│            主地图（可缩放）             │   ▸ 为什么这么判断                 │
│  锋面带·锋面线·冷暖侧·缺测·底图         ├───────────────────────────────────┤
│  ·找鱼范围环·定位点·指针查询浮层         │ L2 当前 | 历史 | 预测 | AI 分析 │
│  [图层] [＋－◎] [选中回执卡]            │    卡片流（随页签切换）             │
├──────────────────────────────────────┴───────────────────────────────────┤
│ 示例数据 · 8 月 5 日（今天）· 观测                          怎么算出来的 ↗    │
└──────────────────────────────────────────────────────────────────────────┘
```

### 3.2 控件唯一性矩阵（v1.1 核心规则）

| 可调维度 | 唯一出现位置 | 明确禁止出现的地方 |
|---|---|---|
| 时间（出海日） | 顶栏「出海日」`#timeDate` + ◀▶ + 播放 + 时间轴（全部同步 `state.date`） | 「历史」页内的日期/月份/年份输入框；「预测」页的独立日期选择 |
| 数据来源（观测/历史/规则预测参考） | 由**页签**决定：当前页=观测、历史页=聚合与同期统计、预测页=规则预测参考、AI 分析=证据组织 | 顶栏不得再有"观测/预报/气候"分段按钮 |
| 找鱼范围 | 顶栏 10/20/30 | 卡片内的半径选择器 |
| ~~目标鱼种~~ | **本期不做**（v1.3 删除） | 曾经在顶栏下拉、也在卡片里出现过鱼种切换——在没有真实渔获/适温数据前，规则偏好会被读成预测 |
| 图层开关 | 地图左下「图层」卡 | 卡片内的图层开关 |
| 统计粒度（当日/3日/7日/15日/当月/整年） | 「历史」页内分段（**唯一例外**：它是"看哪一段统计"，不是日期本身） | 其它页 |

### 3.3 功能排布（保留 / 改 / 并 / 增 / 删）与需求覆盖

| 功能 | 处置 | 覆盖需求 |
|---|---|---|
| 结论卡：三档结论 + 首选/备选点位 + 为什么 | **增**（承接原「预测-渔场可能度」与「AI 分析」结论） | §3.1④、§6、FR-8/9 |
| 唯一时间控件「出海日」（播放 / 日期 ◀▶ / 时间轴，越界钳制） | **改**（替换 4 档尺度 + 三档时间族 + 页内 3 个日期输入框；v1.8 继续保持同一时间真源） | FR-4、FR-16 |
| 顶栏按使用顺序编号分组 | **增** | FR-17 |
| 地图任意点查询（hover 浮层 + 点击钉住 + Esc 取消） | **增** | FR-13/14/15 |
| 选中回执卡（名称/距离/方位 + ✕） | **增** | FR-18 |
| 找鱼范围 10/20/30（唯一范围真源） | **改**（合并散落概念） | §3.1① |
| 目标鱼种选择器 | **删**（v1.3：本期不做，理由见 §3.2 与 §8） | FR-10 |
| 天气与海况（风/浪/涌） | **增**（市场 Must-have；v1.2：无真实数据源，标注"示例数据 · 不参与结论"） | §7 体验、FR-8 |
| 海表温度图层 + 指针查询温度 | **增**（v1.3：接入 NOAA GHRSST 0.05° 逐日真实海温，按 0.5 °C 分档；标明与锋面数据集不同源，不参与评分） | FR-13、FR-20 |
| 陆地 / 海岸线可读性（浅色实色块 + 亮海岸线）+ 默认视野 0.8 | **改**（v1.3：原先陆地填充 `#16283c` 与海面 `#0d2135` 几乎同色、默认视野又看不到海岸线，等于没有底图；现改为明显区分的浅色块 + 亮海岸线，默认视野拉远到东海海岸线在画面内，图例写出「陆地」一行与来源） | FR-19、§3.2 |
| 可达性：航距→航时/油耗 | **增**（FishTrack 卖点） | FR-9 |
| 眼下情况（锋面对象数/距最近锋面/你落在哪侧/数据把握 + 观测覆盖） | **改**（v1.8：当前页默认只留当前渔情和作业线索两张卡，海况与全部清单折叠） | §3.2 |
| 预测逐日规则预测参考（1/3/7 天窗口 + 7 柱 + 7 行，点选即换出海日） | **改**（v1.8：合并为一张预测卡，逐日明细折叠；以当前信号、近几日持续性、历史同期给规则预测参考） | §6.1、FR-5 |
| 历史：当日/3日/7日/15日/当月/整年 | **改**（删掉页内日期输入框；连续窗口、当月与整年展示实况样例聚合和日期格子图，并说明多年样本缺口） | §5.2、FR-4 |
| 数据说明（数据从哪来 / 怎么算的 / 现在还做不到的） | **改**（从页签退到页脚支撑入口，承载数据来源、评分规则、限制项） | §3.2、§5.1 |
| 「AI 分析」页签 | **保留并限责**（v1.8：合并为一张复核卡；只做证据组织、任务编排与证据链展示，不直接生成科学数值） | §3.1④ |
| 原「可信度」卡、像元计数 | **删**（与 §3.2 冲突） | §3.2 |
| 图例：4 个图层开关 + 独立色带 + 一句用法提示 | **改**（开关归开关、色带归色带，不再混排） | §3.2 颜色 |

### 3.4 地图指针查询（v1.1 新增交互；v1.2 改为真实数据）

| 动作 | 结果 |
|---|---|
| 鼠标移到地图任意点 | 浮层跟随指针，列出：坐标 / **这一点在数据里属于哪一类（锋面线及其原始编码 −10/10/30 / 冷侧 / 暖侧 / 无锋面）** / 离你多远+方位 / 是否在找鱼范围内 / 最近锋面的距离+方位+对象编号与长度 / 海表温度（未接入时写「待接入」）/ 当前日期与数据来源 |
| 指针落在没有观测的像元 | 只显示坐标 + 「这里没有观测数据（陆地、云或未观测，数据里统一用 -128 表示）」，不给任何数值 |
| 该日期没有数据 | 只显示坐标 + 原因，不给任何数值 |
| 点一下 | **钉住**该点（地图画十字标记，浮层固定），便于与别处对比 |
| 再点同一点 / Esc / 点 ✕ | 取消钉住 |
| 钉住期间移动鼠标 | 浮层内容保持钉住点不变（不会被指针抢走） |

实现要点：坐标反算走 `svg.getScreenCTM().inverse()`（缩放/裁切都不错位）；`mousemove` 用 `requestAnimationFrame` 节流；浮层 `pointer-events: none` 且位置钳制在地图内；**「这一点是什么」由 `OFData.cellInfo(lon, lat)` 直接查数据里的 RLE 掩码**（不再有本地近似海岸线表）；渔场区半轴用公里（绘制时乘 `KM2PX`），保证"在不在水区内"与卡片数字同源。

---

## 4. 交互规范（11 条）

1. **单向数据流**：`state`（唯一真源）→ `render*()`；渲染函数只读 state，不写 state。所有状态变更集中在事件处理器内。
2. **单一选中语义**：`state.select = {type, id}`；地图要素、结论卡点位、列表条目共用同一选中态（`data-type/data-id`），任何位置点击同一条目结果一致；再次点击取消。
3. **选中必有回执（v1.1）**：选中后地图上出现回执卡（名称 + 距离 + 方位 + `✕`），不再只靠"地图压暗"暗示；`✕` 与 Esc 都能取消。
4. **几何同源**：定位点到锋面/水区的距离、方位由经纬度实时计算（`nearestOnSegment` + `bearing16` + `distKm`）；地图几何与卡片数值不允许出现两套数据；地图投影保证东西/南北向 1 km 等长（否则半径圈不是正圆）。
5. **时间模型（v1.8 收敛）**：全站只有一个时间真源 `state.date`（出海日），可选范围来自 `OFData.availableDates()`，越界自动钳制并提示；日期输入、步进、播放和时间轴都同步这个真源；当前页=观测，历史页=实况聚合与同期统计，预测页=规则预测参考，AI 分析=证据组织。
6. **范围模型**：`state.range` 唯一；半径环、范围内判定、回退规则、文案全部从它派生。
7. **反馈分工**：Toast 只用于"操作确认"（日期/图层/定位/选中）；数据结论一律落在卡片，不用 Toast 播报结论。
8. **渐进披露**：卡片默认只给结论 + 一行说明，细节收进「为什么这么判断 / 和往年比，正常吗」折叠区。
9. **空态与回退**：无数据必须说明原因（覆盖期/窗口）且不给结论，并给出可行动作（如"去看未来"）；找鱼范围内无命中时回退最近对象并**显式标注**"先给最近的"。
10. **指针即数据（v1.1 起）**：鼠标指到地图任意点给出该点**数据里的真实类别**（锋面线及其原始编码 / 冷侧 / 暖侧 / 无锋面 / 没有观测）、距离方位、最近锋面对象编号与长度，点一下可钉住，Esc 取消；落在没有观测的像元或该日期无数据时**只说事实、不给数值**。
11. **控件唯一性（v1.1）**：任一可调维度全站只出现一处（见 §3.2）；顶栏按使用顺序编号分组（① 从哪出发 ② 找多远 ③ 出海日），让"先做什么"看得见（v1.3：目标鱼种下线，顶栏由 4 组收敛为 3 组）。

## 5. 状态模型（`prototype-fishing.js` + `prototype-data.js`）

| 字段 | 说明 |
|---|---|
| `lon, lat` | 定位点（L0 ①） |
| `range` | 找鱼范围 km，取值 10/20/30（L0 ②） |
| ~~`species`~~ | v1.3 删除：本期不做目标鱼种；冷暖侧只展示（数据里的 −20/20），不参与评分 |
| `date` | **唯一的出海日**（L0 ③），可选范围＝`OFData.availableDates()`（即 `data/day/` 实际导出的观测日期） |
| `layers` | `{sst, band, front, coldwarm, nodata, fishing}`（v1.3：新增**真实海温**图层——NOAA GHRSST 0.05° 逐日、按 0.5 °C 分档；v1.2 曾去掉无数据支撑的 SST 色带，现在有真实数据才加回来） |
| `select` | `{type, id}` 或 `null`，全站唯一选中态（对应地图回执卡） |
| `probe` | `{lon, lat}` 或 `null`，被钉住的地图点 |
| `tab` | `now` / `clim` / `future` / `ai` / `basis`（`basis` 由页脚入口进入） |
| `climMode` | `day` / `period` / `week` / `halfmonth` / `month` / `year`：历史看哪一段（锚点来自 `date`） |
| `predWindow` | `1` / `3` / `7`：预测页评估窗口，仍基于 7 天规则预测参考序列 |
| `pickOrigin` | `true` / `false`：是否处于地图点选出发地模式，完成点选或 Esc 后自动退出 |
| `zoom` | 地图缩放（`applyZoom`） |
| 游离状态 | `hoverPt`（指针临时位置，模块级变量，不进入 state） |
| 派生快照 | `snapshot()` 缓存 `{status, ok, fronts, inRange, nearest, cell, quality, objects, spots, score, items, side, coverage}`；state 变更后 `invalidate()` |
| 数据状态 | `OFData.dateInfo(date)`：有没有数据、没有数据的原因（空态文案来源） |
| 数据入口 | **页面只通过 `window.OFData` 取数**（`prototype-data.js`），不直接读 `data/*.js` 的原始结构 |

## 6. 关键判定规则（与「依据」页同一套）

| 项 | 规则 |
|---|---|
| 三档结论 | 数据把握 ≥70% ⇒ 值得去；50–69% ⇒ 可以看看；<50% ⇒ 线索不足（v1.2：海况无数据，不再拿它下结论） |
| 数据把握 | 起评分 30 + 范围内锋面对象×12（最多算 3 个）+ 最近锋面 ≤10 km +8、≤20 km +4 + 范围内最长对象 ≥100 km +8、≥50 km +4 + 观测覆盖 ≥85% +4、<70% −4，clamp 到 5–95（v1.3：删除「鱼种偏好侧别」项——本期不做目标鱼种，冷暖侧只展示） |
| 有锋面的天数占比 | `front_present = 半径内锋面线像元数 > 0`；`probability = 有锋面的天数 / 有效天数`（**唯一口径**） |
| 预测逐日规则预测参考 | 当前把握度 + 近几日持续性 + 历史同期，扣除未接入数据项后给 7 天规则基线；必须显式标注规则预测参考，不能称为真实预报 |
| 锋面对象 | 锋面线像元 → 8 邻域连通域 → 直径路径作中心线 → 按公里抽稀（容差 6 km）→ ≥20 km 才编号（F001…） |
| 航时/油耗 | 距离 ÷ 14.8 km/h；距离 × 1.6 L/km（只看量级） |

## 7. 术语平实化对照（v1.1 全站统一）

| 旧说法 | 现在怎么说 |
|---|---|
| 时次 | 出海日 / 哪一天 |
| 气候（态） | 往年同期 |
| 结论口径 / 口径 | 判断标准 / 怎么算的 |
| 可能度 | 去这里的把握（数值口径不变） |
| 适航判据 | 能不能出海的标准 |
| 作业半径 | 找鱼范围 |
| 温度梯度高于多年同期 | 水温变化比往年明显（**v1.2 停用：海温未接入，评分里不含温度项**） |
| 渔场高概率区 | 值得去的水域 |
| 演示占位 | 示例数据 |
| 确定性扰动 | 上下浮动 |
| 出现率 | 有锋面的天数占比 |
| 作业含义 | 对你的意义 |
| 冷侧 / 暖侧 | 保留（渔民本来就这么说） |
| 海表温度 / SST | 图例用"海表温度"，说明里可简写 SST |

### 7.1 v1.4：界面改成商用平台口吻

原则：**面向用户讲结论与动作**，不写"本页 / 本期 / 数据里…"这类自我说明句；口径与限制集中在「数据说明」页。

| v1.3 及以前 | v1.4 现在怎么说 |
|---|---|
| 锋面对象 | **锋面区**（编号 F001…） |
| 海表温度（界面） | **水温**（「数据说明」页保留"海表温度（NOAA GHRSST）"的精确叫法） |
| 找鱼范围 | **作业范围** |
| 观测覆盖 | **数据覆盖** |
| 数据把握 / 把握 | **把握度** |
| 你落在（哪一侧） | **所处侧** |
| 值得去的水域 | **推荐水域**（示例，默认关闭） |
| 未编号短段 | **短段锋面**（长度不足 20 km，未单独编号） |
| 页签「往年同期」「依据」 | 页签「**历史**」；「**数据说明**」作为页脚支撑入口 |
| 这一天 / 这三天 / 这个月 | **当日 / 近三日 / 当月** |
| 没算进去的 | **未参与评分** |
| 口径 | **统计方式** |
| 示例数据 · 不参与结论 | **示例数据 · 仅供参考** |
| 「为什么这么判断」 | 「**评分依据**」 |
| 像元 / 线像元 / 格点计数 | **不出现在界面上**（只在数据说明与文档里用） |
| 以前按「每天衰减 6%」推过数字，那是编的 | **绝不写进界面**（这是开发史，不是用户信息） |

## 8. 验收清单（已自动化，Edge 无头 + CDP）

`node tools/data-check.mjs`（897 项）+ `node tools/e2e-check.mjs`（108 项）+ `node tools/layout-check.mjs`（25 项），全部 PASS 才算通过。

| 类别 | 断言（要点） |
|---|---|
| 真实数据接入 | 适配层 `OFData` 可读；页面显示产品 DOI 与许可；地图上锋面对象线与数据文件数量一致；日期控件范围＝已导出观测日期 |
| 数据自洽 | 网格/编号/中心线范围；RLE 还原像元数 = `quality` 统计值；有效+缺测 = 总像元；对象像元 ≤ 锋面线像元；`has_sst/has_intensity=false`（锋面文件里本来就没有这两样）；无 NaN/Infinity |
| 海温数据（v1.3） | `sst/` 与 `meta.availability.sst` 声明一致；0.05°、`bin_c=0.5`、游程还原 = `valid_cells`、档位 0~40 °C、无 NaN；与数据源单点值对得上（2024-08-05 @124.525°E/30.025°N = 30.69 °C）；`days.js` 清单与目录一致 |
| 顶栏结构 | 3 个序号分组且标签为 出发地/作业范围/出海日；无 `#familySeg`；只有 1 个日期控件、无 month/number；播放按钮与时间轴同源同步；范围 3 档 |
| 页签层级 | 当前/历史/预测/AI 分析共 4 个；默认落"当前"；数据说明由页脚入口进入 |
| 结论层 | 三档结论文本（值得去/可以看看/线索不足）；结论行含把握度/方位/距离/航时；写明哪天 + 实况观测；首选点位是真实锋面区编号；「评分依据」含起评分、合计与"未参与评分" |
| 现在页 | 4 个指标格（锋面区/最近锋面/所处侧/把握度）；范围标签跟随真源并显示数据覆盖与定位点水温（数值与数据一致）；默认两张卡，海况安全为展开项，默认作业线索不超过 3 条，完整清单仍保留；**温度必须能对回数据文件（不出现数据里没有的温度）** |
| 预测页 | 合并为一张卡；1 / 3 / 7 天窗口 + 7 根规则参考柱 + 7 行逐日建议；写明规则预测参考、真实预报未接入、已有样例日期可点回看实况 |
| 历史页 | 页内 0 个输入框；6 个粒度按钮（当日/3日/7日/15日/当月/整年）；当日/3日含 `front_present` 与实际取样天数；7日/15日/当月/整年有日期格子图，并说明样例范围与多年样本缺口 |
| AI 分析 | 合并为一张复核卡；摘要当前结论、目标锋面、历史参照、规则预测参考、证据边界、下一步建议与证据链；不得出现“AI 生成”的科学数值 |
| 数据说明 | 页脚入口；数据来源 ≥14 行（含 DOI/许可/底图出处/规则预测参考/AI 边界）；评分规则 6 条；使用限制 ≥7 条（含未接入项与审图号提醒） |
| 指针查询 | 指向锋面线像元显示原始编码（与数据一致）；指向缺测像元只说"没有观测数据"且不给数值；含距离/最近锋面/冷暖侧/**该格真实水温（与数据文件一致）**；点击钉住；钉住后鼠标乱动不改内容；再点/Esc 取消；浮层不出地图边界 |
| 底图可读性（v1.3） | 陆地是实色块且填充 ≠ 海面底色；光栅化后陆地像素 > 2 万（默认视野里看得见海岸线） |
| 选中回执 | 点结论卡点位 → 回执卡出现（标题为真实编号）且地图压暗；`✕` 取消后恢复 |
| 联动 | 图层开关（海温/锋面带/缺测）真实重绘；范围 10/30 km 生效；日期切换后地图与全部卡片同步（v1.3：鱼种已下线，不再有「切鱼种改把握」） |
| 边界 | 日期越界钳制到已导出范围上下限；没导出的日期给出空态原因 |
| 覆盖层 | 有数据时地图遮罩 `display:none` 且地图中心可命中（不是只查 `hidden` 属性）；真没数据时才出现并说明原因 |
| 地图交互（v1.3） | 滚轮/按钮缩放改变视窗且比例尺跟着换算；真实度数经纬网（°E/°N）；「回到定位点」复位视窗 |
| 其他 | 方向键切页签；无运行期 JS 异常（含资源加载失败）；1680/1280 无横向溢出与文字裁切；顶栏单行；图例 6 图层；选中卡与缩放按钮不重叠；比例尺与图例不重叠 |

## 9. 待确认问题

1. 主用户优先级：近海小渔船（日航次，重安全/成本）优先，还是远洋/专业捕捞（重鱼群定位）优先？
2. 「AI 分析」后续是否需要接真实本地大模型？当前已作为独立页签保留，但只做本地证据组织。
3. 目标鱼种清单是否固定为东海常见 4 种？是否需要按海区配置？（**v1.3 已下线该功能**：等有真实渔获/适温数据再接回，届时需重新定义偏好权重的数据来源）
4. 原"单日/连续多日/整月/整年"四档时间尺度是否还需要放到顶栏？当前 v1.8 已放在「历史」页内，顶栏只保留一个出海日真源，并提供播放/时间轴。
5. 海况（风/浪/涌）是否有真实数据源？若有，应把"能不能出海"升为 L1 必答项并替换示例值。
6. 指针查询是否需要"一次钉多个点"做对比（当前支持 1 个钉住点 + 指针临时查看）？
7. **海表温度来源**：接 Copernicus C3S（需账号）还是先用 NOAA ACSPO 免登录产品打样？
   → **v1.3 已定**：先用 **NOAA CoastWatch ERDDAP `noaacwBLENDEDCsstDaily`**（GHRSST Geo-Polar Blended 夜间融合，0.05° 逐日，免账号、可离线留存子集）打通到页面；页面与「依据」页都标明**与锋面数据集（ESA CCI/C3S）不是同一产品**。要同源再走 Copernicus C3S（`copernicusmarine` 命令模板已在 `Ocean/backend/app/data_preparation.py`）。
8. **往年同期要补多少历史日期**？按月补齐后「这个月」口径才能真正出数（当前每年只取 3 天）。
   → v1.3 现状：样本＝2015—2024 的 8 月 5—7 日（个别年缺 1 天，页面按「有效天数」算并写明抽样口径）；日期页面已扩到 2024-07-01 ~ 08-31 共 62 天，「整月」口径仍需按月补历史样本。
9. **渔场数据与许可**：GFW 公开数据是非商业授权，是否申请商业许可 / 改用合作方 AIS 数据？
10. **国内发布的底图合规**：正式版是否要换带审图号的合规底图？
