# 需求规格：面向渔场预测的海洋锋面分析系统

> 版本：v0.10（前端简洁化版）
> 日期：2026-09-14
> **v0.10 变更**：① 当前页默认收敛为“当前渔情 + 作业线索”两张卡，海况安全与全部锋面区清单改为展开项；② 预测页合并为一张卡，窗口、柱图、首选日先呈现，逐日明细放入展开项；③ AI 分析合并为一张复核卡，任务编排与证据链默认折叠，降低信息密度。
> **v0.11 规划**：引入 Global Fishing Watch 风格的表观捕捞活动（apparent fishing effort）作为渔业响应信号。第一阶段先以 2024-07-01 至 2024-08-31 东海样例闭环为目标，新增“历史 AIS 响应”证据块和“相似锋面事件响应”模块；界面继续使用“渔场可能性 / 作业线索”，不使用“产量预测”作为承诺。
> **v0.9 变更**：① 出发地补齐“地图点选”入口，与坐标输入写同一个定位真源，坐标输入兼容方向标注与中文逗号；② 推荐水域示例层默认关闭，避免被误读为真实渔场数据；③ 预测页界面文案统一为“规则预测参考”，减少技术词外露；④ 历史连续窗口、当月、整年增加日期格子图，强化连续 / 月 / 年尺度表达。
> **v0.8 变更**：① 顶栏「出海日」组增加播放 / 暂停与时间轴拖动，仍写同一个 `state.date`；② 「历史」补充 7 日、15 日连续实况窗口，与当日、3 日、当月、整年共同覆盖单日 / 连续 / 月 / 年尺度；③ 「预测」增加 1 / 3 / 7 天窗口切换和可展开预测依据；④ 「AI 分析」增加证据链展开，标明当前、历史、预测和限制证据来源。
> **v0.7 变更**：① 右侧页签按 9.3 沟通记录收敛为「当前 / 历史 / 预测 / AI 分析」，「数据说明」改为页脚支撑入口；② 「历史」补齐当日 / 近三日 / 当月 / 整年四个时间尺度，其中当月与整年使用已导出实况样例并说明多年样本缺口；③ 「预测」落地 7 天规则预测参考（当前锋面信号 + 近几日持续性 + 历史同期），明确不等于真实业务预报；④ 「AI 分析」作为本地证据组织与任务编排，不生成新的科学数值。
> **v0.6 变更**：① 目标鱼种（FR-10）暂缓（本次下线）——顶栏由 4 组收敛为 3 组（FR-17），把握评分删除「鱼种偏好侧别」项，冷暖侧只按数据展示；② 顶栏「出海日」的可选范围与 `data/day/` 已导出日期自动同步（本次为 2024-07-01 ~ 08-31 共 62 天），不再写死；③ 地图覆盖层（无数据遮罩）只在真无数据时出现；④ **海温接入真实数据**（NOAA GHRSST 0.05° 逐日，与锋面不同源，按 0.5 °C 分档展示、不参与评分）；⑤ 地图观感升级：整数度经纬网、比例尺、滚轮缩放/拖拽平移、陆地与海上缺测分层、RLE 消缝、锋面线显示层平滑。
> 依据：`9.3.md` 定位更新、原课题文档（1-12 周）、数据实测结论与数据契约（`docs/data-schema.md`）、市场与文献调研（见 `docs/ux-spec.md` §1）
> 仓库：本仓（原型与需求独立托管）+ `Ocean/backend`（数据导出脚本 `scripts/export_prototype_data.py`）

---

## 1. 项目定位

**面向渔场作业的海洋锋面分析系统**：以海洋锋面为核心环境因子，向渔民/渔业管理者提供"渔场在哪里、何时去、风险如何"的决策支持。

系统回答三类问题：
1. 当前位置/目标海域，**现在**有没有值得关注的锋面与渔场线索？
2. 这一带**过去**（近 1 月/1 年/历史同期）锋面活动规律如何？
3. **未来**（短临/数日）锋面会如何演化、渔场可能性如何变化？

> 定位转变对照：v0.1 定位是"海洋锋面时空任务分析工具"（面向科研演示）；v0.2 定位转向"渔场作业辅助"，一切功能围绕"渔场决策"收敛。

---

## 2. 范围与排除

### 2.1 本期范围（1-2 个月里程碑）
- 重做信息架构：三层结构 + 四页签（当前 / 历史 / 预测 / AI 分析），数据说明作为支撑入口
- 定位半径内锋面 + 渔场综合展示；自由缩放与按需加载
- 时间尺度扩展：单日 → 连续多日 → 整月 → 整年（**v0.10：顶栏仍只有一个出海日真源，历史页内提供 3 / 7 / 15 日、当月、整年尺度，并用格子图呈现连续性**）
- 查询性能优化（经纬度定位加载提速）
- 界面精简与配色优化；移除与决策无关的工程数据
- 预测算法定义与原型（历史 + 冷暖结构 + 锋面强度 等多因素）
- v0.11 规划：以 GFW 表观捕捞小时数作为第一阶段渔业响应真值，先完成 2024-07-01 至 2024-08-31 的样例闭环，再扩展到 2015-2024 每年 8 月研究样本。

### 2.2 明确排除（后续版本）
- 三维地球视图（延后）
- AIS/渔场数据接入与分析视图（数据到位后单独视图）
- 真实本地大模型推理（可暂以规则 AI 兜底）
- 手机端、离线安装包

---

## 3. 界面信息架构（首页/主工作台）

> v0.3 起改为**三层结构**：L0 全局上下文（唯一真源）→ L1 结论层（常驻）→ L2 分层细节（页签）。
> 详细规范与调研依据见 `docs/ux-spec.md`。

```
┌──────────────────────────────────────────────────────────────────────┐
│ L0 顶栏：① 从哪出发 · ② 找多远(10/20/30km) · ③ 出海日(◀ 日期 ▶)          │
├──────────────────────────────────┬───────────────────────────────────┤
│                                  │ L1 结论层（常驻 hero）：            │
│                                  │   可否出海 + 首选点位 + 为什么       │
│     主地图（二维，可缩放）           ├───────────────────────────────────┤
│   SST/等温线/锋面/冷暖侧/渔场区       │ L2 页签：                          │
│   /作业半径环/定位点/指针查询浮层      │   当前 · 历史 · 预测 · AI 分析       │
└──────────────────────────────────┴───────────────────────────────────┘
```

**四条硬约束**
1. **时间、范围、出发地只有一处真源**（L0 顶栏）；卡片内不得再出现第二套时间/范围控件。时间维度只有"出海日"一个控件，观测 / 历史 / 规则预测参考 / AI 证据组织由页签决定。
2. **结论先于细节**：L1 常驻，任何页签切换都不隐藏结论；结论必须可执行（方位+距离+航时）。
3. **无数据不编造**：缺数据时给明确原因与可行动作，不给结论（FR-7）。
4. **控件唯一性**：任一可调维度（时间/范围/图层）全站只出现一处（FR-16）。

### 3.1 右侧功能分区（四类功能保留，职责重新划分）

| 分区 | 层级 | 回答的问题 | 主要内容 | 明确不做 |
|---|---|---|---|---|
| ① 当前 | 主任务 | 这里当前是什么情况？ | 眼下情况（水温 / 距最近锋面 / 所处侧别 / 去这里的把握）、作业线索；天气海况与全部锋面清单放入展开项 | 不放多年统计，不默认铺满长清单 |
| ② 历史 | 支撑 | 过去这片海有什么规律？ | 当日 / 3 日 / 7 日 / 15 日 / 当月 / 整年六档；同期 `front_present` 与已导出实况样例并列，样本不足直接说明 | **页内不得有日期控件**（锚点用顶栏"出海日"） |
| ③ 预测 | 主任务 | 未来 7 天里哪天更值得关注？ | 规则预测参考（当前锋面信号 + 近几日持续性 + 历史同期）、最好的一天、7 天柱图；逐日明细可展开 | 不冒充真实业务预报 |
| ④ AI 分析 | 支撑 | 如何把当前/历史/预测证据组织成建议？ | 一张复核卡：本地证据摘要、下一步建议；任务编排和证据链可展开 | 不生成新的科学数值，不重复复杂概率口径 |

> 说明：v0.7 按 9.3 沟通记录恢复「AI 分析」独立入口，但职责限定为证据组织与任务编排；结论仍保留在 L1，数据来源与限制仍集中在页脚「数据说明」。


### 3.2 界面清理（依据 9.3.md 原话落实）
| 原话 | 落成需求 |
|---|---|
| "135 个数据文件的图标…删去" | 移除文件计数/数据文件统计类 UI |
| "概率口径相关内容删去…统一普适算法" | 历史频率用**唯一口径**（见 §5.3），不再展示多重可信度标签 |
| "显示过多过程数据…精简去除" | 面板默认只呈现结论级信息；计算过程折叠进"详情" |
| "颜色优化更改" | 重新设计配色：SST 使用标准色带，锋面/冷暖侧用高辨识度色，明确图例 |

---

## 4. 功能需求

### 4.1 时空交互（前端）

| 编号 | 需求 | 验收标准 |
|---|---|---|
| FR-1 | 地图以定位点为圆心，默认加载半径内全部图层；半径外按需加载 | 首次定位 ≤ 2s 显示核心图层；拖动/缩放再取周边瓦片 |
| FR-2 | 支持缩放/平移，缩放无死区，不整页刷新 | 操作流畅（交互帧率可用），无数据区给"加载中/无数据"提示 |
| FR-3 | 位置输入与地图点选两种定位方式 | 输入经纬度或点选均可定位，两种方式结果一致；v0.10 已落地“点选”按钮与地图点击更新出发地，并兼容 `124.50°E, 30.20°N` / `30.20N，124.50E` 两类输入 |
| FR-4 | 时间由 L0 顶栏**唯一**真源"出海日"表达（日期 + ◀▶ 步进 + 播放 / 暂停 + 时间轴；范围＝`data/day/` 已导出的观测日期，本次 2024-07-01 ~ 08-31，越界自动钳制并说明原因）；旧"观测/预报/气候"三档一律取消 | 任一日期变化后地图与全部卡片同步；全站只有 1 个日期输入框，播放与时间轴同步同一个 `state.date`，页内没有任何日期输入框 |
| FR-5 | 时空信息完整：同一页内可看"当前（观测）/ 历史（过去）/ 预测（规则预测参考）/ AI 分析"，四者说法一致（见 §5.3 唯一口径） | 四个页签直达；"当前"页只讲观测，"预测"页标注规则预测参考，不需要另找地方切换数据来源 |
| FR-6 | 渔场信息预留叠加层（数据到位后启用） | 图层开关存在；数据就绪后可显示点位/范围 |
| FR-7 | 低信息密度提示：无数据/超出范围给出明确提示，不展示编造值 | 无数据与有数据的展示可区分 |

### 4.2 视图组织（三层 / 四页签，对齐 9.3.md 原话"过去/当前/预测"）

| 层/页签 | 内容 | 进入方式 |
|---|---|---|
| L1 结论层 | 这天值不值得去 + 首选/备选锋面对象（方位/距离/航时/油耗）+ 为什么这么判断 | 常驻，首页默认可见 |
| ① 当前 | **真实锋面 + 真实海温**：找鱼范围内锋面对象数 + 距最近锋面 + 你落在哪一侧（数据里的 −20/20 编码）+ 数据把握 + 观测覆盖 + 定位点真实海温；默认只显示作业线索，天气海况（**示例，不参与结论**）和锋面对象清单折叠 | 首页默认页签（观测数据） |
| ② 历史 | 当日 / 3 日 / 7 日 / 15 日 / 当月 / 整年：真实多年度 `front_present` 占比、已导出实况样例聚合、连续日期格子图、样本缺口提示 | 页签「历史」（页内无日期控件） |
| ③ 预测 | 未来 7 天规则预测参考：支持 1 / 3 / 7 天窗口切换，当前锋面信号、近几日持续性、历史同期共同给出可解释基线；逐日明细可展开，可点已有样例日期回看实况 | 页签「预测」 |
| ④ AI 分析 | 当前结论、目标锋面、历史参照、规则预测参考、限制项的本地证据组织；任务编排与证据链可展开 | 页签「AI 分析」 |
| 数据说明 | 数据从哪来（含 DOI/许可/底图出处）+ 怎么算的 + 现在还做不到的 | 页脚「查看数据说明 ↗」 |

**新增功能需求（v0.3，市场调研驱动）**

| 编号 | 需求 | 依据 |
|---|---|---|
| FR-8 | L1 结论必须包含"可否出海"三档判断，并把安全置于可能度之上 | 渔民决策链第 0 问；安全优先 |
| FR-9 | 结论必须给出可执行点位：方位 + 距离 + 预计航时（+油耗量级） | FishTrack"省油/省时"卖点；可达性即成本 |
| FR-10 | ~~目标鱼种可选，并影响可能度权重（适温区间、冷暖侧偏好）~~ **v0.6 暂缓**：没有真实渔获/适温数据前不做；冷暖侧照数据展示、不参与评分 | §1 定位；不同鱼种适温带不同（等真实数据再启用） |
| FR-11 | 无数据/超覆盖时必须说明原因与可行动作，且不给结论 | FR-7 强化，防止编造 |
| FR-12 | 半径内无命中时回退最近对象，并显式标注"已回退" | 保证信息不中断且不误导 |

**新增功能需求（v0.4，交互可用性驱动）**

| 编号 | 需求 | 说明 |
|---|---|---|
| FR-13 | **地图任意点查询**：鼠标移到地图上任意一点，即显示该点的坐标、**数据里这一格是什么（锋面线及其原始编码 −10/10/30 / 冷侧 / 暖侧 / 没有观测）**、**该格真实海温（v0.6 起接入 NOAA GHRSST，按 0.5 °C 档位；该格没有海温就直说）**、与定位点的距离和方位、是否在作业半径内、最近锋面（对象编号 + 长度） | 渔民常问"这一片怎么样"；只有热力图看不出数值 |
| FR-14 | 点一下可**钉住**该点（对比两点），Esc 或点击同一点取消 | 满足"对比两个地方"的作业习惯 |
| FR-15 | 鼠标落在没有观测的像元（陆地/云/缺测，数据里同为 −128）时明确提示"这里没有观测数据"，不给任何数值 | 与 FR-11 同源：不编数值 |
| FR-16 | **控件唯一性**：任一可调维度（时间 / 范围 / 图层）在界面上只出现一处；时间维度收敛为顶栏"出海日"单控件，取消"观测/预报/气候"三档与页内日期控件 | 原实现有 3 处能改时间、20+ 可点元素，主次不清 |
| FR-17 | 顶栏控件按使用顺序分组编号（① 从哪出发 ② 找多远 ③ 出海日；v0.6 起目标鱼种下线，由 4 组收敛为 3 组） | 让"先做什么"看得见 |
| FR-18 | 任何"选中"都要有回执：选中对象后地图上出现卡片，标明名称、距离、方位并带取消按钮 | 原实现只有地图压暗，用户不知道自己选了什么 |

### 4.3 渔场专项（延后，见 §8）
AIS 渔业数据到位后，新增"渔场视图"：作业船位热力 → 实际渔场高频区 → 与锋面距离分布 → 渔场时空规律。不进入本期验收。

v0.11 的渔业响应路线不直接使用“渔获量”或“产量”作为真值，而使用 GFW 风格的 apparent fishing effort，即由 AIS 行为估计得到的表观捕捞小时数。产品界面将其表达为“历史 AIS 响应”“表观捕捞活动响应”或“作业线索”，并在数据说明中解释其不是真实渔获量。

> 许可提醒：Global Fishing Watch 公开数据为 **CC BY-NC 4.0（仅非商业）**，商用需另行授权；在拿到许可或合作方数据之前，地图上的"值得去的水域"只能作为**示例占位**并显式标注。

**新增功能需求（v0.5，真实数据接入驱动）**

| 编号 | 需求 | 验收标准 |
|---|---|---|
| FR-19 | **来源可追溯**：锋面位置/冷暖侧/对象编号都来自真实数据集（Zenodo 20356239，CC BY 4.0） | 「依据」页显示产品名、DOI、许可、分辨率与生成时间；地图与卡片数字都能对应到 `data/` 文件 |
| FR-20 | **未接入的变量显式标注**：锋面强度、海况、预报、渔场一律标「待接入」或「示例」，不得插值/以示例值冒充；**海温（v0.6 起）已接入真实数据**（NOAA GHRSST 0.05° 逐日，按 0.5 °C 分档展示），但必须标明与锋面数据集不同源，且不参与把握评分 | 页面文本里不出现插值温度；把握评分项只含锋面数据 |
| FR-21 | **时间范围由数据覆盖决定**：可选日期＝`data/day/` 已导出的观测日期；越界自动钳制并提示原因 | 顶栏日期控件 min/max 与数据一致；没导出的日期给出明确空态说明 |
| FR-22 | **往年同期只用唯一口径**，并公开抽样范围（哪几年、哪几天、共几天） | 页面展示 `front_present = 半径内锋面线像元数 > 0` 与抽样说明；缺样本的年份显示「缺测」而不是编造值 |
| FR-23 | **数据文件可校验**：结构与数值自洽由自动化断言保证 | `node tools/data-check.mjs` 全绿（网格/编号/中心线范围/RLE 还原像元数 = 统计值/无 NaN） |

---

## 5. 后端与数据

### 5.1 数据源与口径（以已确认事实为准）
| 数据 | 用途 | 现状 |
|---|---|---|
| `front` 锋面位置（Zenodo **20356239**，0.05° 逐日，CC BY 4.0） | 锋面线 / 冷暖侧 / 锋面对象 | **已接入**：东海 120–128°E、27–34°N，观测 2024-07-01 ~ 08-31（62 天逐日）；对象＝连通域临时编号（F001…） |
| 往年同期样本（同一产品） | 同期统计（唯一口径） | **已接入**：2015—2024 的 8 月 5—7 日（个别年缺 1 天）；口径 `front_present = 半径内锋面线像元数 > 0` |
| Natural Earth 1:10m（陆地/海岸线/200 m·1000 m 等深线） | 底图、陆海参照 | **已接入**（公有领域）；海陆/缺测判定改用数据里的 `-128` |
| `frontal_intensity` 锋面强度 | 强度分级、预测特征 | 未接入（43 个分年包约 100 GB；对数还原公式见数据契约） |
| `analysed_sst`（v0.6：**NOAA CoastWatch/GHRSST** `noaacwBLENDEDCsstDaily`，0.05° 逐日，度 C） | SST 色带、指针查询温度 | **已接入**（免账号，`scripts/fetch_sst_samples.py`）；按 0.5 °C 分档展示、不参与评分；**与锋面数据集不同源**（锋面用 ESA CCI/C3S SST；要同源可换 Copernicus C3S，需账号） |
| GEBCO 水深 | 更精细的等深线 | 未接入（可选升级项） |
| 海况（风/浪/涌） | 出海判断 | 未接入：页面保留但标注「示例数据 · 不参与结论」 |
| AIS 渔业分析数据 | 渔场图层（未来） | 未接入：GFW 公开数据为 **CC BY-NC 4.0（非商业）**，商用需另行授权；本演示用示例占位并标注 |

v0.11 计划新增：

| 数据 | 用途 | 规划 |
|---|---|---|
| GFW apparent fishing effort（日尺度 0.01°） | AIS 作业响应、历史 AIS 响应证据、相似锋面事件响应分析 | 第一阶段手工获取 2024-07-01 至 2024-08-31 东海样例；前端展示聚合到 0.05°，缓冲区分析保留 0.01° 计算粒度；第一版不区分船旗国和渔具类型 |

### 5.2 AIS 作业响应分析口径（v0.11 规划）

- **空间匹配**：围绕锋面事件计算 10 / 20 / 30 km 缓冲区内的 apparent fishing effort，界面默认 20 km，研究报告展示三半径敏感性。
- **时间窗口**：展示锋面事件前 7 天至后 7 天，默认解释后 1-3 天响应。
- **响应增强**：后 1-3 天 fishing hours 至少比前 7 天均值高 20%，且高于同日非锋面对照区域。
- **对照区域**：同日、同海区、离锋面至少 50 km 的同面积非锋面区域。
- **相似事件**：同一海区、前后 15 天季节窗口内的历史锋面事件；形态、长度、方向、强度作为后续增强特征。
- **产品评分**：GFW 数据接入后，“渔场可能性”由当前锋面线索和历史 AIS 响应共同构成，第一版权重为 50% / 50%，实现和回测后再决定是否固化。
- **公开边界**：GFW 衍生数据先用于本地/课堂演示和聚合图展示，不公开发布底层数据文件。

> 数据落盘与生成流程、字段级结构、已知坑：见 `docs/data-schema.md`。

### 5.3 历史概率统一口径（依据 9.3.md）
- 只保留一个口径：**该点/该范围在所选时间窗内出现锋面的占比**。
  `频率 = 窗口内有锋面的有效日期数 / 窗口内有效日期总数`
- 时间窗支持：连续 N 日、指定月份、指定年份、历史同期（多年同月同日）。
- 移除多重概率定义、覆盖可信度等复杂标签；注释中保留计算公式供查证。
- v0.10 落地情况：已实现「当日 / 3 日 / 7 日 / 15 日 / 当月 / 整年」六个入口；当日与 3 日使用多年同期统计，7 日 / 15 日 / 当月 / 整年使用 2024-07-01 ~ 08-31 已导出实况样例聚合，并通过日期格子图展示连续尺度，同时明确提示多年连续窗口 / 整月 / 全年样本缺口。
- v0.6 现状：单日地图图层扩到 2024-07-01 ~ 08-31 共 62 天；同期样本仍为 2015—2024 的 8 月 5—7 日
  （个别年缺 1 天，按「有效天数」算并写明抽样口径）。

### 5.4 后端需求

| 编号 | 需求 | 说明 |
|---|---|---|
| BR-1 | 区域预裁剪 + 本地缓存 | 首次按 bbox 读取后缓存为本地切片/瓦片，避免重复读全球网格 |
| BR-2 | 分层按需渲染：重要信息优先 | 首帧=海岸线+底图，其次 SST，再叠加锋面/对象，最后统计面板 |
| BR-3 | 地形/海岸线本地存储 | 不依赖在线底图服务，全部本地数据 |
| BR-4 | 加载性能：定位点经纬度查询提速 | 目标：1° 窗口核心信息首屏 ≤ 2s；需先定位瓶颈（IO/解码/渲染） |
| BR-5 | 时间聚合接口 | 单日/月/年三类聚合服务，避免前端逐日拉取 |
| BR-6 | 三维预留 | 渲染与数据接口保持与地图实现解耦，便于未来替换球面视图 |

### 5.5 前端工程需求
- 保持 React + MapLibre（当前栈），不引入重型地图库；
- 组件按"当前/历史/预测/AI"分区拆分，降低单文件体积（现状 App.tsx 过大，影响维护）。

---

## 6. 预测（本期为"算法定义 + 原型验证"，暂缓产品化）

### 6.1 预测目标
未来 L 天（L=1/3/7）某格点/区域**出现锋面（进而作为渔场线索）的可能性**。

### 6.2 预测输入（多因素，与 9.3.md 一致）
| 因素 | 说明 |
|---|---|
| 过去情况 | 历史同期锋面出现频率（§5.3 口径） |
| 冷暖情况 | 近期冷暖侧结构变化、SST 距平 |
| 锋面强度 | `frontal_intensity` 与局地温度梯度 |
| （后续）流场/风场 | GLORYS12 / ERA5，数据到位后加入 |

### 6.3 方法建议
- 先建立基线：持续性（今天有锋面→明天概率）+ 气候态（历史频率）。
- 在基线上迭代规则/统计/机器学习模型，要求可解释、可回看。
- 创新点定位（9.3.md）：设计**综合过去、冷暖、强度等多因素的复合概率算法**，与单一因素基线对比给出提升。

### 6.4 验收标准
- 预测接口返回可解释的"未来 L 天概率场 + 依据说明"；
- 有离线回测（用历史日期切分验证），指标可复现。

---

## 7. 性能与体验（9.3.md："查找经度纬度时加载过慢"）

| 目标 | 场景 | 指标 |
|---|---|---|
| 定位首屏 | 输入经纬度后 | ≤2s 出当前视图核心图层 |
| 尺度切换 | 单日 → 整月/整年 | ≤3s 出聚合结果（命中缓存时 ≤1s） |
| 地图漫游 | 缩放/平移 | 无整页加载感，无数据时有占位 |
| 首包体积 | 生产构建 | 无明显大 chunk 警告（现状 1.2MB 需代码分割） |

优化方向（按收益排序）：切片/瓦片缓存 → 聚合接口 → 按需加载 → 前端分包。

---

## 8. 演进路线（Roadmap）

| 阶段 | 内容 | 依赖 |
|---|---|---|
| P0（本次） | 界面精简 + 三维时间视角 + 统一口径 + 性能优化 + 预测原型 | 现有 1-12 周代码 |
| P1 | GFW apparent fishing effort 样例闭环：历史 AIS 响应证据块、相似锋面事件响应模块、事件响应表 | GFW 样例数据 / 数据使用边界确认 |
| P2 | 真实 SST/intensity 接入、数据审计、预测回测 | Copernicus 账号 / 全量数据 |
| P3 | 渔场视图深化：多年份 AIS 响应、锋面-渔业活动响应模式挖掘 | GFW 或合作方 AIS 数据 |
| P4 | 三维地球视图、真实本地 LLM、业务化预测产品 | 以上完成后 |

---

## 9. 与 9.3.md 逐条映射（追溯）

| 9.3.md 原话 | 需求条款 |
|---|---|
| 定位=面向渔场预测 | §1 |
| 135 个文件图标删去 | §3.2 |
| 概率口径删去/统一普适 | §5.3 |
| 日期支持连续/月/年 | §4.1 FR-4/5（v0.10：顶栏保持唯一「出海日」并支持播放 / 时间轴，连续 / 月 / 年尺度放在「历史」页内，页内不再有日期控件） |
| 右侧只保留 当前/历史/预测/AI | §3.1（v0.10 已落地为当前 / 历史 / 预测 / AI 分析；数据说明退为支撑入口） |
| "AI 分析"给出结论与建议 | §3.1④（v0.10：AI 分析做本地证据组织与证据链展示，核心结论仍常驻 L1） |
| 过去/当前/预测 三尺度 | §4.2（v0.10：当前=观测，历史=当日/3日/7日/15日/当月/整年，预测=规则预测参考） |
| 创新点=综合多因素概率预测 | §6.3 |
| 3D 球体延后 | §2.2、§8 P4 |
| AIS 数据做另一视图 | §4.3、§8 P1/P3 |
| 加载慢需优化 | §7、BR-4 |
| 颜色优化 | §3.2 |
| 界面过程数据精简 | §3.2 |

---

## 10. 待确认问题（提给老师/合作方）

> v0.5 更新：数据侧问题 1、2、8、9 已推进或明确结论（下方标注），新增 10–13。

1. 渔场预测的目标区域与覆盖海域？（东海 / 中国近海 / 全球）
   → **已定**：当前按东海（120–128°E / 27–34°N）落地，可扩窗口。
2. AIS 渔业数据的具体内容与字段？渔场"真相"如何定义（作业船位聚类 / 渔获量 / 渔区报告）？
   → **待定 + 许可风险**：Global Fishing Watch 公开数据为 **CC BY-NC 4.0（仅非商业）**，且是"作业时长"而非渔获量；
      若要走这条线，需先确认项目是否非商业 + 是否申请 API/商业许可。
3. "预测"的功能形态：地图概率场，还是点位的概率+建议？
4. 是否保留离线演示需求（无外网）？这影响地形与图层缓存策略。
   → v0.5 已按"离线可用"实现：数据是预生成文件 + `<script>` 引入，双击即可跑，不依赖在线底图服务。
5. 预测结果是否需要有可解释性展示（用于向渔民解释"为什么这里可能高产"）。
   → v0.3 已按"必须可解释"落地：L1「依据与判定规则」逐项列出加分项；结论口径写入「依据」页。
6. 目标用户优先级：近海小渔船（日航次，看重安全与油钱）优先，还是远洋/专业捕捞（看重鱼群定位精度）优先？这决定 L1 结论的措辞与权重。
7. 顶栏原有"单日/连续多日/整月/整年"四档尺度是否为评审硬性要求？
   → v0.10 已收敛：顶栏只留「出海日」一个日期控件，但同组内提供播放和时间轴；连续 / 月 / 年尺度放在「历史」页内，预测页给规则预测参考。若评审要求把四档放回顶栏，会与"控件唯一性"（FR-16）冲突，需先确认取舍。
8. 海况（风/浪/涌）是否有可用的真实数据源？若有，应把"出海判断"从演示占位升级为 L1 必答项。
   → v0.5 现状：仍无数据源；页面保留但标注「示例数据 · 不参与结论」，L1 结论不含海况判断（不拿假数据给安全建议）。
9. 地图指针查询是否需要**一次看多点**（比如同时钉住 3 个候选点做对比）？当前原型支持钉 1 个点 + 鼠标临时查看。
10. **海表温度来源**：要不要接 Copernicus C3S（需账号，与锋面同源 0.05°）？还是先用 NOAA ACSPO 免登录产品打样？
    → **v0.6 已定**：先用 NOAA CoastWatch ERDDAP `noaacwBLENDEDCsstDaily`（GHRSST 融合，0.05° 逐日，免账号）打通；页面与「依据」页均标明与锋面数据集（ESA CCI/C3S）不同源；要同源再换 Copernicus C3S（需账号）。
11. **多给一批历史日期**：要不要按月补齐逐年样本（再下 15—30 天，约 20—40 MB）？补了之后「往年同期」能按整月口径算。
    → **v0.6 已扩**：单日地图图层 2024-07-01 ~ 08-31 共 62 天（raw 约 80 MB，留本地不入库）；往年同期样本 2015—2024 的 8 月 5—7 日（2017-08-07、2019-08-05 曾因 Zenodo 断流缺失，已重试补齐，缺的年份页面显示「缺测」）。按月补齐后「整月」口径才能真正出数。
12. **底图合规**：将来在国内正式发布，底图需换成有审图号的合规底图（自然资源部标准地图服务）；原型期用 Natural Earth（公有领域）。
    → 待业务方确认发布形态。
13. **锋面线编码语义**（−10/10/30 的确切物理含义）仍未权威确认；原型只原样显示编码、不做解释。

> 界面层待确认问题另见 `docs/ux-spec.md` §9。
