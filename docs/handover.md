# 交接文档 · 海洋锋面渔情原型（渔场向导）

> 交接基线：本文件所在提交（以 `git log -1` 为准）；此前关键提交 `f985845`（真实数据 + 水温图层）、`9c5b9c0`（陆地可见性 + 文案平实化）。
> 读者：接手开发 / 维护 / 对外演示的人。看完应能做到：本地跑起来、改文案与样式、更新数据、知道哪些没做、避开已知的坑。

## 1. 这是什么

东海一带（120–128°E / 27–34°N）的**渔场线索原型**：给定「出发地 / 作业范围 / 出海日」，回答三件事——这一带有没有锋面、值不值得跑一趟、先去哪一条。

技术形态：**纯前端静态页面**。1 个 HTML（结构与样式）+ 2 个 JS（状态机与渲染、数据适配层）+ 一组脚本生成的 JSON-in-JS 数据文件。无构建、无框架、无外部依赖、不联网，双击 HTML 即可运行。

## 2. 先跑起来 / 怎么算"改对了"

```powershell
# ① 打开页面（无需服务器）
start prototype-fishing.html

# ② 三项检查（Node 18+；e2e 需要本机装有 Edge）
node tools/data-check.mjs     # 数据文件结构与自洽：890 项断言
node tools/e2e-check.mjs      # 交互端到端（无头 Edge + CDP）：100 项断言
node tools/layout-check.mjs   # 布局几何（1280 / 1680 两档）：23 项断言
```

三项都必须 **0 失败**才算改对；`e2e-check` 每次会覆盖写截图到 `%TEMP%\shot-1-now.png` … `shot-10-empty.png`（Windows 上通常是 `C:\temp\`），可以直接拿来看页面长什么样。

检查脚本本身就是回归网：数据一致性、地图是否真的画出来（含光栅化后数像素）、交互状态、文案关键口径、布局是否重叠或裁切。**改文案时如果断言挂了，先判断是"文案变了断言该更新"还是"信息丢了"**——口径类关键词（起评分、数据覆盖、front_present、-128、审图号等）是故意被断言的。

## 3. 已实现的功能

### 3.1 地图（唯一的信息主区）

| 能力 | 实现说明 | 验证 |
|---|---|---|
| 真实底图 | Natural Earth 1:10m 陆地（浅色实色块 `#3a5068`）/ 海岸线（亮色 `rgba(196,229,252,.9)`）/ 200 m·1000 m 等深线；默认视野缩放 0.8，保证海岸线在画面内 | e2e：陆地填充 ≠ 海面底色；光栅化后陆地像素 > 2 万 |
| 真实水温场 | NOAA GHRSST 0.05° 逐日，按 0.5 °C 分箱 → 逐行游程 → 同档相邻行合并绘制 | e2e：路径数 = 数据文件里的档位数 |
| 真实锋面 | 锋面带（原始像元）、锋面区中心线 + 编号（F001…，标注真实长度）、冷侧 / 暖侧（−20 / 20 编码） | e2e：地图对象数 = 数据文件对象数 |
| 无观测掩码 | 数据里 −128 用斜线纹理，**只画在海上**（陆地画在其上，避免把陆地也标成"缺测"） | e2e：缺测层存在且与陆地分层 |
| 读数辅助 | 真实整数度经纬网（带 °E / °N 标注）、比例尺（随缩放实时换算）、10 / 20 / 30 km 作业范围环、出发地标记；出发地可输入坐标（支持方向标注与中文逗号），也可进入“点选”模式后点地图设置 | e2e：格网线 ≥ 4 条且带 °E/°N；比例尺随缩放变化；点地图后坐标输入与结论同步 |
| 视图操作 | 滚轮缩放（以指针为中心）、拖拽平移、◎ 回到出发地（含复位默认缩放） | e2e：缩放后 viewBox 变化、复位后与初始一致 |
| 图层开关 | 6 个可切换图层（水温 / 锋面带 / 锋面区 / 冷暖侧 / 无观测 / 推荐水域）；推荐水域是示例层，默认关闭；图例写明陆地来源 | layout：图例 6 行、开关可点；e2e：示例渔场不在首屏默认展示 |
| 任意点查询 | 鼠标悬停**只显示 4 项**：经纬度 / 该格真实水温（NOAA GHRSST，与锋面不同源）/ 最近锋面距离 / 是否锋面区（是则冷暖侧）；缺测像元只说「这里没有观测数据」，不给任何数值；**点击钉住**（十字标记），Esc 或再点取消 | e2e：锋面区判定与数据同源、水温与数据一致、浮层恰好 4 项（坐标 + 3 行信息）且不含方位/编码/推荐水域 |
| 选中回执 | 点任何目标（地图要素、结论卡、清单）→ 地图上出回执卡（名称 / 长度 / 距离 / 方位 / ✕），全站共用一套选中语义 | e2e：地图点击、卡片点击、✕/Esc 取消 |

### 3.2 结论与各页签

| 区域 | 现在能做什么 |
|---|---|
| 结论块（**2026-09-14 起并入「当前」页，不再常驻**） | 三档结论（值得去 / 可以看看 / 线索不足）+ 作业范围内锋面区数量与首选锋面区（方位 / 距离）+「评分依据」展开逐项加分与合计，并写明数据来源与未参与评分的项；无数据时只说明原因、不给结论 |
| 当前 | 默认两张卡：当前渔情 4 个指标（锋面区数 / 最近锋面 / 所处侧 / 把握度）+ 作业线索；范围标签显示作业范围 / 数据覆盖 / 定位点水温；海况安全（**示例数据 · 仅供参考**）和全部锋面区清单放入展开项 |
| 历史 | 当日 / 3 日 / 7 日 / 15 日 / 当月 / 整年六种尺度；当日与 3 日保留同期统计口径，7 日 / 15 日 / 当月 / 整年展示已导出的实况样例和连续日期格子图，并明确说明多年连续窗口 / 整月 / 全年样本缺口 |
| 预测 | 一张预测卡：未来 7 天规则预测参考，可切换 1 / 3 / 7 天窗口，使用当前锋面信号 + 近几日持续性 + 历史同期；逐日明细可展开，已有样例日期可点回看实况，并标明不等于业务预报 |
| AI 分析 | 一张复核卡：把当前结论、历史参照、规则预测参考和限制项汇总成任务建议；任务编排与证据链可展开，不生成新科学数值，不调用在线模型 |
| 数据说明 | 页脚入口；数据来源（含 DOI / 许可 / 底图 / 版本 / 规则预测参考与 AI 边界）、评分规则（5 条）、使用限制（逐条列出未接入项与合规要求） |

### 3.3 交互与可访问性

- 唯一真源：时间只有顶栏「出海日」；范围只有一处 10/20/30；图层只有图例开关。页面内不再出现第二套控件。
- 单向数据流：`state` → `render*()`，渲染只读 state；所有变更集中在事件处理器。
- 页签 `role=tab` + ←/→ 方向键切换；Esc 取消钉住/选中；Toast 只做操作回执，不播报结论。
## 4. 数据与算法口径（改结论前必读）

### 4.1 数据源

| 用途 | 来源 | 许可 / 口径 | 覆盖 |
|---|---|---|---|
| 锋面位置 / 冷暖侧 | Zenodo `10.5281/zenodo.20356239`（全球逐日中尺度锋面数据集，上海海洋大学 Xing 等） | CC BY 4.0；0.05° 逐日；变量 `front` | 已导出 62 天：2024-07-01 ~ 2024-08-31 |
| 水温 | NOAA CoastWatch ERDDAP `noaacwBLENDEDCsstDaily`（GHRSST L4，夜间融合） | GHRSST free and open；0.05° 逐日；按 **0.5 °C 分箱**展示，不插值 | 同 62 天 |
| 底图 | Natural Earth 1:10m 陆地 / 海岸线 / 等深线 | 公有领域（可商用，无需署名） | 裁剪到 117.5–131.5°E / 25–35°N 并抽稀 |
| 历史同期统计 | 由锋面数据样本离线统计 | 2015–2024 每年 8 月 5—7 日，共 30 天样本 | 页内写明抽样口径 |

**编码含义**（数据集原始值，页面照实展示）：`-10 / 10 / 30` 锋面线（三者语义在数据说明中仍有歧义，页面统一当“锋面线”画）、`-20` 冷侧、`20` 暖侧、`-128` 缺测（**同时含陆地、湖泊、云**，无法区分“没有锋面”与“没有观测”）。

**范围与网格**：120–128°E / 27–34°N；起点 0.025（半个格），步长 0.05°，160 × 140 格；出发地默认 124.50°E, 30.20°N。

### 4.2 把握度评分（页面上唯一的"可算"数字）

```
把握度 = clamp(30 + 四项加分, 5, 95)          # 起评分 30
① 范围内锋面区数量：每个 +12，最多计 3 个
② 最近锋面距离：≤10 km +8，≤20 km +4
③ 范围内最长锋面区：≥100 km +8，≥50 km +4
④ 数据覆盖（= 100 − 缺测率）：≥85% +4，<70% −4
三档：≥70% 值得去 · 50–69% 可以看看 · <50% 线索不足
```

**不参与评分**：水温、锋面强度、海况、预报、渔场；冷暖侧只展示。这是刻意的取舍——没有真实渔获/适温数据时，把偏好算进分数会被读成预测。

### 4.3 其他口径

- **锋面区编号**：锋面线像元做连通域 → 取直径路径当中心线 → 按 6 km 抽稀 → 长度 ≥ 20 km 才编号（`F001`…）。编号是本系统内部编号，不是数据集自带的轨迹编号。
- **历史同期 `front_present`**：半径内锋面线格数 > 0 记为出现；比例 = 出现天数 ÷ 有效天数。这是历史同期页唯一口径，缺样本的年份不参与分母。
- **结论块位置**：只出现在「当前」页「当前渔情」卡内（2026-09-14 由常驻 hero 并入）；**「航时 / 油耗」估算已删除**，页面不再给出任何基于巡航速度/油耗系数的推算值。
- **投影与距离**：演示投影保证东西向与南北向等长（1 km = 1 km），所以范围环是正圆；距离 / 方位由经纬度实时计算，地图上的线与卡片数字同源。

## 5. 欠缺的功能（按"要补什么"分组）

### 5.1 数据缺口（直接影响结论可信度）

| 缺什么 | 现在页面怎么处理 | 补齐路径 |
|---|---|---|
| **锋面预报**（预测） | 「预测」页已给规则预测参考，并清楚标注不是业务预报 | 接预报输入场（水温 / 流场 / 风场），补强度场与回测指标后替换当前规则基线 |
| **真实海况**（风 / 浪 / 涌） | 当前页展开项标「示例数据 · 仅供参考」，并提示以官方海洋预报为准 | 接 ECMWF / NOAA WW3 / ERDDAP 风浪场；接入后应把出海安全提升为 L1 必答项 |
| **锋面强度** | 「未接入」 | 数据集里的 `frontal_intensity` 变量尚未下载，需补下载 + 导出编码 |
| **渔场 / 船位**（GFW 等） | 地图上是示例椭圆，标注「示例数据」 | GFW 许可为 CC BY-NC 4.0，**商用需单独授权**；先明确使用场景再谈数据 |
| **水温与锋面同源** | 页面标明两者来自不同产品 | 换 Copernicus C3S SST（0.05°，与锋面同源）需账号与下载配额 |
| **历史同期覆盖太窄** | 「当月 / 整年」展示当前实况样例，同时说明多年整月 / 全年统计缺口 | 按月、按年补齐逐日历史样本后，才能输出真正多年整月 / 全年统计 |
| **历史日期不可步进** | 出海日只能选 2024-07-01 ~ 08-31 | 需要时把 2015–2024 的 8/5–7 也导出到 `data/day/`，即可在日期上直接对比 |

### 5.2 功能缺口

- 时序/动画：已支持顶栏播放与时间轴拖动；更复杂的多点对比动画、轨迹残影和导出视频尚未做。
- 输出与协作：没有导出图片 / 分享链接 / 报告；没有账号与收藏。
- 合规地图：国内正式发布必须换成带**审图号**的合规底图（当前 Natural Earth 仅用于原型演示）。GitHub Pages 因此**故意没有开启**。
- 移动端：只做了 1280 / 1680 两档布局断言，未做触屏手势与窄屏（< 900px）适配。
- 法规信息：禁渔期、航行通告、船位报告等没有接入。

### 5.3 工程缺口

- **加载体积**：62 天全量数据 3.24 MB，以 `<script>` 全量注入。日期规模再上一个量级就需要改按需加载（按日期分片 / fetch 动态导入）。
- **没有 CI**：三项检查靠手动跑；建议接 GitHub Actions，把 `node tools/*.mjs` 串成流水线。
- **没有构建/类型**：改动靠断言兜底，重构（如拆分渲染模块）前应先补断言。
- **统计与渲染耦合**：历史同期的计算逻辑写在渲染函数内，若要复用需先抽离。
## 6. 交接事项

### 6.1 目录与文件职责

```
ocean-front-prototype/
├── prototype-fishing.html      结构 + 样式（L0 顶栏 / 地图 / L1 结论 / L2 页签）
├── prototype-fishing.js        状态机 + 渲染 + 交互（唯一改动入口，只从 OFData 取数）
├── prototype-data.js           数据适配层：页面唯一数据入口 window.OFData
├── data/                       脚本生成的数据（**不要手改**）
│   ├── meta.js                 数据产品 / 许可 / 可用日期 / 已知问题（数据说明读它）
│   ├── days.js                 清单：已导出的锋面与水温日期（HTML 按它注入 <script>）
│   ├── day/<日期>.js           单日锋面：对象中心线、锋面带、冷暖侧、缺测掩码、质量统计
│   ├── sst/<日期>.js           单日水温：按 0.5 °C 分箱的逐行游程
│   ├── clim/same-period.js     历史同期统计（front_present 口径）
│   └── base/basemap.js         底图（Natural Earth 裁剪抽稀结果）
├── tools/
│   ├── build-basemap.mjs       拉 Natural Earth → 裁剪抽稀 → data/base/basemap.js
│   ├── data-check.mjs          数据断言（890）
│   ├── e2e-check.mjs           交互断言（97，无头 Edge + CDP，产出截图）
│   └── layout-check.mjs        布局断言（22）
└── docs/
    ├── handover.md             本文件
    ├── data-schema.md          数据契约（字段 / 编码 / 生成方式 / 已知问题）
    ├── requirements-fishing-ground.md   需求规格（v0.10）
    └── ux-spec.md              信息架构与交互规范（v1.8）
```

上游数据脚本在**另一个仓库**：`Ocean/backend/scripts/`（`fetch_zenodo_front_samples.py`、`fetch_sst_samples.py`、`export_prototype_data.py`）。原型仓只消费 `data/`，不包含抓取逻辑。

### 6.2 环境与常用命令

```powershell
# 上游（Ocean 仓）：虚拟环境在 Ocean/backend/.venv（已装 netCDF4、requests）
cd Ocean\backend

# ① 拉数据（示例：一天 / 一段日期）
.\.venv\Scripts\python.exe scripts\fetch_zenodo_front_samples.py 2024-08-05
.\.venv\Scripts\python.exe scripts\fetch_sst_samples.py 2024-08-05 2024-08-06

# ② 导出到原型仓 data/
.\.venv\Scripts\python.exe scripts\export_prototype_data.py            # days 模式：扫描 data/raw 全量导出
.\.venv\Scripts\python.exe scripts\export_prototype_data.py --mode clim --clim-dates ...   # 历史同期统计
# 快通道（只重生成 meta.js / days.js，几十秒）：
.\.venv\Scripts\python.exe scripts\export_prototype_data.py --mode days --dates 2024-08-05 --no-sst --out-dir ..\..\ocean-front-prototype\data

# ③ 底图（需要网络，走 jsDelivr 上的 Natural Earth）
node tools\build-basemap.mjs

# ④ 三项检查（原型仓）
node tools\data-check.mjs; node tools\e2e-check.mjs; node tools\layout-check.mjs
```

### 6.3 数据更新流程（照这个顺序做）

1. `fetch_zenodo_front_samples.py <日期…>` 拉到 `Ocean/data/raw/front/`；水温用 `fetch_sst_samples.py` 拉到 `Ocean/data/raw/sst/`。
2. 跑 `export_prototype_data.py`（可加 `--dates` 只导指定日期；`--no-sst` 跳过水温）。
3. 跑三项检查；`data-check` 会验证新日期的结构与自洽，`e2e-check` 会验证页面确实用上了新数据（日期范围、对象数与文件一致）。
4. 提交 `data/**` 与（如改了）`docs/**`、`tools/**`；`data/**` 一律由脚本产出，不要在编辑器里改。

### 6.4 四条硬约束（改代码前先接受）

1. **不手改生成文件**：`data/**`、`data/base/basemap.js` 都是脚本产物；要改内容就改脚本再重跑。
2. **不编数字**：没有数据源的变量一律标「未接入」或「示例数据 · 仅供参考」，不得插值、不得用样例值冒充真实值；预测页只能写明是规则预测参考，不能冒充业务预报。
3. **缺数据要明说**：无观测、超范围、样本不足都必须给出原因与可行动作（换日期 / 放大范围 / 先补数据），并且不给结论。
4. **单一真源**：时间 / 范围 / 图层在界面上各只出现一处；新增功能不得再引入第二套控件。

### 6.5 已知坑（踩过一次就别再踩）

| 坑 | 现象 | 规避方式 |
|---|---|---|
| 中文路径 + netCDF-C | `netCDF4` 打不开含中文的路径（本仓库路径含「海洋锋面」） | 导出脚本内部已做临时 ASCII 路径中转；自己写脚本时同样处理 |
| CSS `display` 覆盖 `hidden` | 地图遮罩一直显示，页面上看不到数据 | 已加 `[hidden]{display:none!important}`；新增覆盖层不要再写 `display` 覆盖 |
| 只断言 `el.hidden` 的测试会漏 | 63 项全过但页面实际不可用 | e2e 用 computed style + `elementFromPoint` 命中判断 |
| PowerShell 读文件乱码 | `Get-Content` 默认按 ANSI | 用 `Get-Content -Encoding UTF8`；工具输出用 `Out-File -Encoding utf8` |
| Zenodo 下载中断 | `ProtocolError` / 半截文件 | 抓取脚本已带重试 + 重新打开归档 + `.part` 原子写入 |
| `git credential fill` 交互 | 在工具终端里会挂起 | 不要交互式调用；需要凭据时用非交互方式读取 |
| 无头 Edge 偶发不稳 | CDP 连接超时 | e2e 启动后轮询 `/json/list`，并等待页面就绪标记再断言 |
### 6.6 当前状态：待你决定 / 未推送的东西

| 事项 | 现状 | 建议 |
|---|---|---|
| 原型仓远端 | `origin` = `JustPlayinger/ocean-front-prototype`，`demo` = `JustPlayinger/ocean-front-demo`（**public**），两者 `main` 同步 | 若担心数据/品牌外泄，把 demo 仓改为 private 或删除 |
| GitHub Pages | **故意未开启** | 国内发布需先换合规底图 + 审图号；内网演示可直接双击 HTML |
| Ocean 仓脚本 | `fetch_zenodo_front_samples.py`（已修改）、`export_prototype_data.py`、`fetch_sst_samples.py`（未跟踪）**尚未提交** | 该仓是协作者共享仓（`NingRuo-code/Ocean`，当前在 `chore/collab-baseline` 分支），提交前先与协作者确认分支；否则至少把这 3 个脚本备份/冻结到原型仓的 `tools/upstream/` |
| `data/meta.js` 与脚本版本 | meta 的「已知问题」文案由 `export_prototype_data.py` 生成 | 若上游脚本长期不受控，重跑导出会覆盖文案；改文案要改脚本 |
| **UI 基线来源** | 本版页面结构（当前 / 历史 / 预测 / AI 分析 + 时间轴播放 + 地图点选）来自协作者仓 `NingRuo-code/Ocean-front-prototype-f` 的提交 `e9c55db`（对齐 9.3 沟通），在我们 `ac06676` 之上 fast-forward 合入 | 该远端已登记为 `friend`；后续同步用 `git fetch friend && git diff friend/main` 看增量 |
| **待办：预测页缺回测** | 「预测」页给出 7 天规则参考（5–85% + 依据完整度），口径已在页内与使用限制中标注"不等于业务预报"，但**没有回测指标**（命中率 / 与持续性基线对照） | 接入预报输入场前，先补规则基线与「今天有→明天有」持续性基线的命中率对照；没有回测就把百分数降级为风险窗口提示 |
| **待办：AI 页命名** | 「AI 分析」实际是本地证据组织与任务编排（页内已标"本地规则"、不生成新科学数值） | 建议改名或加副标（如「分析过程 · 本地规则，不调用在线模型」），避免被读成有模型推理 |
| **待办：历史「整年」口径** | 「整年」按钮展示的是样例年实况聚合，不是完整全年统计（文档已写明，界面按钮文字仍易误读） | 按钮改「样例年」或在结果区首行写明"非完整年度统计" |

### 6.7 三分钟演示脚本（对外讲的时候照这个顺序）

1. **定位与范围**（顶栏）：出发地 124.50°E / 30.20°N，作业范围 20 km → 「当前」页结论块立刻给出：范围内锋面区数量 + 把握度 + 三档判断 + 首选锋面区（方位 / 距离）。
2. **地图**：指出海岸线与陆地（浅色实色块）、水温色带（NOAA GHRSST）、红色锋面区中心线与编号、蓝色冷侧 / 橙色暖侧、斜线是"无观测"；滚轮放大到某个锋面区，指出比例尺跟着变。
3. **任意点查询**：鼠标停在锋面上，只显示「经纬度 / 水温 / 最近锋面距离 / 是否锋面区（是则冷暖侧）」四项，点一下钉住，Esc 取消。
4. **换日期 / 播放**：点「出海日」◀▶ 或播放按钮，拖动时间轴，展示不同日期的锋面位置变化（这是真实观测回算，不是预报）。
5. **预测页**：展示 1 / 3 / 7 天规则预测参考窗口，并强调它来自当前锋面信号、近几日持续性和历史同期，不是业务预报。
6. **历史页**：当日 / 3 日 / 7 日 / 15 日 / 当月 / 整年，连续窗口和月/年视图带日期格子图，说明哪些是真实样例聚合，哪些需要补多年样本。
7. **AI 分析**：展开证据链，说明它只是本地证据组织与任务编排，不生成新科学数值。
8. **数据说明**：数据来源（DOI / 许可）、评分规则、使用限制（含"国内发布需合规底图"）。

**常见追问与标准回答**

- 「这数据准不准？」→ 锋面是 Zenodo 上公开的 0.05° 逐日卫星锋面数据集（CC BY 4.0），水温是 NOAA GHRSST 融合产品；两者不同源，页面已标明。
- 「预测 7 天准不准？」→ 当前是规则预测参考，只用于演示预测工作流；真实业务预报还需要预报输入场、锋面强度和离线回测。
- 「能不能直接上线？」→ 需要三件事：合规底图与审图号、真实海况数据、以及数据规模上来后的按需加载。

### 6.8 术语表

| 界面用词 | 含义 | 数据里的对应 |
|---|---|---|
| 锋面区（F001…） | 一条被编号的锋面中心线对象 | `objects[]`，长度 ≥ 20 km 才编号 |
| 短段锋面 | 太短、未单独编号的锋面线 | `front_lines` 里未进入 `objects` 的部分 |
| 锋面带 | 数据集原始锋面像元（−10 / 10 / 30） | `front_band_rle` |
| 冷侧 / 暖侧 | 锋面两侧的水团 | `-20` / `20` |
| 无观测 | 缺测（同时含陆地、湖泊、云） | `-128`，`nodata_rle` |
| 把握度 | 唯一参与评分算出的百分比 | 前端按 §4.2 公式计算 |
| 数据覆盖 | 100 − 缺测率 | `quality.nodata_percent` |
| 水温 | 海表温度（NOAA GHRSST，0.5 °C 分档） | `sst/*.js` 的游程与 `sstCell()` |
| 推荐水域 | 示例渔场占位（无真实数据，默认关闭） | `DEMO_SPOTS`（前端常量） |
| 历史同期 | 往年同一日期的锋面出现比例 | `clim/same-period.js`，`front_present` 口径 |
| 规则预测参考 | 未来 7 天本地规则基线 | 当前锋面信号 + 近几日持续性 + 历史同期 |
| AI 分析 | 证据组织与任务编排 | 本地规则，不生成科学新数值 |

### 6.9 验收基线（交接时应当复现）

| 项 | 期望值 |
|---|---|
| `node tools/data-check.mjs` | 0 失败 / 890 项 |
| `node tools/e2e-check.mjs` | 0 失败 / 100 项（截图写入 `%TEMP%`） |
| `node tools/layout-check.mjs` | 0 失败 / 23 项 |
| 数据规模 | `data/` 3.24 MB；`day/*.js` 62 个、`sst/*.js` 62 个 |
| 日期范围 | 2024-07-01 ~ 2024-08-31（62 天，逐日） |
| 历史同期样本 | 2015–2024 每年 8/5–7，共 30 天 |
| 默认视图 | 缩放 0.8，可见经度约 121.0–128.0°E，东海海岸线在画面内 |

任何一项对不上，先查数据是否重导过、`data/days.js` 清单是否与 `data/day/` 一致，再查页面是否读的是同一份 state。



